{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "blueprint-schema.json",
  "title": "Nextcloud Playground Blueprint",
  "description": "Declarative configuration for provisioning a Nextcloud Playground instance at boot.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "$schema": { "type": "string" },
    "meta": {
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "author": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "debug": {
      "type": "object",
      "properties": { "enabled": { "type": "boolean" } }
    },
    "landingPage": {
      "type": "string",
      "description": "Path to open after boot, e.g. /index.php/apps/dashboard or /index.php/login."
    },
    "siteOptions": {
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "locale": { "type": "string" },
        "timezone": { "type": "string" }
      }
    },
    "admin": {
      "type": "object",
      "description": "Admin account created by occ maintenance:install.",
      "properties": {
        "username": { "type": "string" },
        "password": { "type": "string" },
        "email": { "type": "string" }
      }
    },
    "apps": {
      "type": "array",
      "description": "Shorthand for enabling bundled apps; each entry expands to an enableApp step.",
      "items": { "type": "string" }
    },
    "steps": {
      "type": "array",
      "description": "Provisioning steps executed (in order) after install.",
      "items": {
        "type": "object",
        "required": ["step"],
        "properties": {
          "step": {
            "type": "string",
            "enum": [
              "enableApp",
              "disableApp",
              "createUser",
              "createGroup",
              "addUserToGroup",
              "setConfig",
              "installApp",
              "writeFile",
              "runOcc"
            ]
          },
          "app": { "type": "string" },
          "appId": { "type": "string" },
          "url": {
            "type": "string",
            "description": "Source URL: the app ZIP for installApp, or the file contents for writeFile (CORS-enabled host required)."
          },
          "enable": {
            "type": "boolean",
            "description": "installApp: enable the app after extracting it (default true)."
          },
          "path": {
            "type": "string",
            "description": "writeFile: target path, relative to the Nextcloud root or absolute."
          },
          "content": {
            "type": "string",
            "description": "writeFile: file contents (text, or base64 when encoding is set)."
          },
          "encoding": {
            "type": "string",
            "enum": ["utf8", "base64"],
            "description": "writeFile: how `content` is encoded (default utf8)."
          },
          "username": { "type": "string" },
          "password": { "type": "string" },
          "displayName": { "type": "string" },
          "email": { "type": "string" },
          "groups": { "type": "array", "items": { "type": "string" } },
          "group": { "type": "string" },
          "key": { "type": "string" },
          "value": {},
          "args": { "type": "array", "items": { "type": "string" } }
        }
      }
    }
  }
}
